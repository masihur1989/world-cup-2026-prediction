# Dashboard runtime only — this is what the deployed app (Streamlit Cloud /
# container) installs. The dashboard reads pre-computed CSVs and runs no model
# code, so it needs nothing from the training stack.
# For training / running the pipeline / tests, use requirements-train.txt.
pandas==2.2.2
numpy==1.26.4
streamlit==1.35.0
plotly==5.22.0
