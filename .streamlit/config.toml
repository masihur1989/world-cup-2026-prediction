# Keep the toolbar/menu (Rerun, Settings, Print, About) but hide developer-only
# items — notably the Deploy button and Edit/source options.
[client]
toolbarMode = "viewer"

[browser]
gatherUsageStats = false
