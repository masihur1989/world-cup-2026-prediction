# Full stack: training the models, running the pipeline, and the test suite.
# Includes the dashboard runtime deps via the line below.
-r requirements.txt
statsmodels==0.14.2
scipy==1.13.1
xgboost==2.0.3
optuna==3.6.1
shap==0.45.1
scikit-learn==1.4.2
mlflow==2.13.0
pytest==8.2.0
setuptools<81  # shap/numba still import pkg_resources, removed in setuptools 81+
